import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function UsersPage() {
  return (
    <PlaceholderPage
      title="Users"
      phase="Soon"
      description="Role-aware user management — curator, reviewer, administrator — with future SSO."
    />
  );
}
