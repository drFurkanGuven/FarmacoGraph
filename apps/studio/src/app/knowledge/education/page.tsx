import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function EducationPage() {
  return (
    <PlaceholderPage
      title="Education"
      phase="Studio 4.2"
      description="Educational layer editor — summaries, pearls, flashcards — isolated from biomedical truth."
    />
  );
}
